// Optional: set Supabase defaults here so you don't have to paste every time.
// You can commit these (anon key is public), or keep empty and paste in the UI.
window.BJ_SUPABASE_URL = "";            // e.g. "https://xxxx.supabase.co"
window.BJ_SUPABASE_ANON_KEY = "";       // Settings -> API -> anon public key
